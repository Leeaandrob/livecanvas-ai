# ===========================================
# LiveCanvas AI - Environment Variables
# ===========================================
# Copy this file to .env and configure your values
# cp .env.example .env

# ===========================================
# FRONTEND (apps/web)
# ===========================================

# Worker host for WebSocket and API connections
# In development: leave empty to use proxy, or set to "localhost:8787"
# In production: set to your Cloudflare Worker domain
# Example: "live-canvas-worker.your-domain.workers.dev"
VITE_WORKER_HOST=

# Worker URL for Vite proxy (development only)
# Default: http://localhost:8787
VITE_WORKER_URL=http://localhost:8787

# WebSocket URL for Vite proxy (development only)
# Default: ws://localhost:8787
VITE_WS_URL=ws://localhost:8787

# ===========================================
# BACKEND (apps/worker)
# ===========================================

# Environment mode
# Values: development, staging, production
ENVIRONMENT=development

# ===========================================
# LLM API KEYS (Client-side - stored in browser localStorage)
# ===========================================
# NOTE: API keys are NOT stored in environment variables for security.
# Users enter their own API keys in the Settings modal, which are stored
# in the browser's localStorage and sent with each AI request.
#
# Supported providers:
# - OpenAI (gpt-4o, gpt-4o-mini, etc.)
# - Anthropic (claude-3-opus, claude-3-sonnet, etc.)
# - Google Gemini (gemini-2.0-flash, gemini-1.5-flash, etc.)
#
# To get API keys:
# - OpenAI: https://platform.openai.com/api-keys
# - Anthropic: https://console.anthropic.com/
# - Google AI: https://makersuite.google.com/app/apikey

# ===========================================
# CLOUDFLARE (Production deployment)
# ===========================================

# Cloudflare Account ID (for wrangler deploy)
# Find at: https://dash.cloudflare.com/ > Workers & Pages > Overview
# CLOUDFLARE_ACCOUNT_ID=

# Cloudflare API Token (for CI/CD)
# Create at: https://dash.cloudflare.com/profile/api-tokens
# CLOUDFLARE_API_TOKEN=
